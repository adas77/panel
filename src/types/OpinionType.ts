export type OpinionType = {
    widget?: boolean,
    src?: string,
    id: number,
    alt: string,
    rate: number,
    name: string,
    surname: string,
    date: Date,
    description: string,
}
